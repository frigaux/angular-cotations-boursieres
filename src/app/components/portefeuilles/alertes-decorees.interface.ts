import {AlerteDecoree} from './alerte-decoree.interface';

export interface AlertesDecorees {
  alertesDecorees: Array<AlerteDecoree>;
  avecOperandeMIN: boolean;
  avecOperandeMAX: boolean;
  avecOperandeMOY: boolean;
}
