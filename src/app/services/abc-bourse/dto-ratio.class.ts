export class DTORatio {
  annee: number;
  per: number;
  bnpa: number;

  constructor(annee: number, per: number, bnpa: number) {
    this.annee = annee;
    this.per = per;
    this.bnpa = bnpa;
  }
}
